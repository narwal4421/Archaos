[build]
builder = "dockerfile"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
startCommand = "sh -c 'npx prisma migrate deploy && npx prisma db seed && node dist/main'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "archaos-api"
